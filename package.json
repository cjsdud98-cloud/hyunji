{
  "name": "hyunji-matjip",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "build": "node --check server.mjs && node --check functions/_shared/nearby-api.mjs",
    "pages:dev": "wrangler pages dev ."
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "wrangler": "^3.114.0"
  }
}
