{
  "name": "hyunji-matjip",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "build": "node --check server.mjs && node --check lib/nearby-api.mjs",
    "pages:dev": "npx wrangler pages dev ."
  },
  "engines": {
    "node": ">=18"
  }
}
